# Ignore everything, except these tracked files
*
!start.sh
!start-dspark.sh
!stop.sh
!README.md
!CHANGELOG.md
!.env.sample
!LICENSE
!.gitignore
!bench/
!bench/bench.sh
!bench/ndec.py
# DFlash2 path (2026-08-19): serving script, image builds, and the overlay
!start-dflash.sh
!start-ornstein.sh
!stop-ornstein.sh
!prepare-ornstein.sh
# DFlash2 build machinery (self-contained builder + patch + overlay)
!patch/
!patch/**
# Deliberately NOT whitelisted: hybrid scripts (start-hybrid.sh,
# start-mtp-8889.sh, hybrid-router.py) and analysis docs (DS4F/KIMI/GROK/
# TIER_*). HANDOFF.md stays local — never un-ignore it.
HANDOFF.md
